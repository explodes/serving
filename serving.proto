syntax = "proto3";

package serving;

option go_package = "github.com/explodes/serving";


message Timestamp {
  oneof duration {
    int64 milliseconds = 1;
  }
}

message BindAddress {
  string host = 1;
  uint32 port = 2;
}